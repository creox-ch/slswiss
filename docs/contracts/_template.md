# Contract: <название фичи>

**Спринт:** `<feature-slug>`
**Дата:** `<YYYY-MM-DD>`
**Статус:** `draft` | `review` | `final` | `in_progress` | `done`

---

## Цель

<1-2 предложения: что делаем и зачем. Без техники.>

## Не входит в спринт

<Что мы НЕ делаем в этом спринте — чтобы не разрастаться.>

- ...
- ...

## Зависимости

- **Зависит от:** <что должно быть готово до начала>
- **Блокирует:** <что мы разблокируем этим спринтом>

---

## Пользовательские сценарии

Конкретные действия, которые юзер может выполнить. Каждый = одно проверяемое утверждение.

- [ ] Юзер делает X → видит Y
- [ ] Юзер нажимает Z → состояние меняется на W
- [ ] ...

## Edge cases

Граничные и редкие случаи. Что должно НЕ произойти.

- [ ] При невалидном вводе появляется ошибка `<точный текст>`
- [ ] Дублирование `<действия>` не создаёт дубликат в БД
- [ ] ...

## UI-инварианты

Как выглядит и ведёт себя интерфейс. Мобильная адаптация.

- [ ] Элемент `<X>` видим на экранах ≥320px
- [ ] Кнопка `<Y>` меняет текст на `<Z>` после клика
- [ ] Модалка закрывается по Esc и клику вне
- [ ] ...

## Технические инварианты

- [ ] Нет ошибок в `console.error` при стандартном flow
- [ ] Не сломаны существующие проходящие тесты
- [ ] Размер `soiludi_v4.html` не вырос больше чем на `<N>KB`
- [ ] ...

---

## Definition of Done

Все галочки выше → отмечены.
Все Playwright-тесты этого спринта → зелёные в CI.
Архитектурный документ `slswiss-architecture.md` обновлён, если архитектура изменилась.
Спринт отмечен `done` в `BACKLOG.md`.

---

## Решения и заметки

<Всё что всплыло по ходу: «решили цену 29», «отложили X на следующий спринт», «evaluator предложил Y, отказались потому что Z».>

---

## История

- **<дата>** — создан Planner-чатом
- **<дата>** — критика от Evaluator (что добавили)
- **<дата>** — финализирован
- **<дата>** — closed
